---
name: canary
description: Canary verification wrapper for Garry Tan's gstack. Use for post-deploy smoke checks and rollout validation.
user-invocable: true
---

# canary

This is the OpenClaw wrapper for gstack canary.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/canary/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
