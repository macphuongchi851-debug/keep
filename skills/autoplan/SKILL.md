---
name: autoplan
description: Auto-review workflow wrapper for Garry Tan's gstack. Use to run a full multi-review planning pass.
user-invocable: true
---

# autoplan

This is the OpenClaw wrapper for gstack autoplan.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/autoplan/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
