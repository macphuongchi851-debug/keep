---
name: document-release
description: Release documentation wrapper for Garry Tan's gstack. Use to sync docs, changelogs, and release notes after shipping.
user-invocable: true
---

# document-release

This is the OpenClaw wrapper for gstack document-release.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/document-release/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
