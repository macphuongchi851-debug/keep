---
name: setup-deploy
description: Deployment setup wrapper for Garry Tan's gstack. Use for preparing deployment wiring, config, and release prerequisites.
user-invocable: true
---

# setup-deploy

This is the OpenClaw wrapper for gstack setup-deploy.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/setup-deploy/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
