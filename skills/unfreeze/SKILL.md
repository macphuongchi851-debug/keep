---
name: unfreeze
description: Unfreeze wrapper for Garry Tan's gstack. Use to remove edit restrictions after a freeze workflow.
user-invocable: true
---

# unfreeze

This is the OpenClaw wrapper for gstack unfreeze.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/unfreeze/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
