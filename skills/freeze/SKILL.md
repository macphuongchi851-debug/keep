---
name: freeze
description: Scope-freeze wrapper for Garry Tan's gstack. Use to limit edits to a specific area or module.
user-invocable: true
---

# freeze

This is the OpenClaw wrapper for gstack freeze.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/freeze/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
