---
name: land-and-deploy
description: Landing and deploy wrapper for Garry Tan's gstack. Use for merge/deploy workflows with validation steps.
user-invocable: true
---

# land-and-deploy

This is the OpenClaw wrapper for gstack land-and-deploy.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/land-and-deploy/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
