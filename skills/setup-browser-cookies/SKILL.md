---
name: setup-browser-cookies
description: Browser-cookie setup wrapper for Garry Tan's gstack. Use when a workflow needs authenticated browser cookies.
user-invocable: true
---

# setup-browser-cookies

This is the OpenClaw wrapper for gstack setup-browser-cookies.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/setup-browser-cookies/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.
