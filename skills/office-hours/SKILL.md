---
name: office-hours
description: Brainstorm and pressure-test a product idea using Garry Tan's gstack office-hours workflow. Use for idea exploration, product thinking, and early-stage validation.
user-invocable: true
---

# office-hours

This is the OpenClaw wrapper for gstack office-hours.

Before doing the workflow, read and follow:
- `{baseDir}/../gstack/office-hours/OPENCLAW.md`

Use OpenClaw-native tools first: `exec`, `read`, `write`, `edit`, `browser`, `web_search`, and `web_fetch`.

## OpenClaw fast path
- Run this as a structured chat conversation.
- Ask one focused question at a time and use `web_search`/`web_fetch` only when helpful.
