# 2026-03-06

- 昨晚/今早排查 OpenClaw Gateway “打不开/反复重启”问题：systemd/journalctl 显示配置校验失败：`models.providers.ollama.models` 期望 array 但为 undefined。
- 根因：某次配置把 `models.providers.ollama` 加入了 `baseUrl/apiKey/api`，但缺少 `models: [...]` 数组，导致 gateway 启动直接退出。
- 从备份 `~/.openclaw/openclaw.json.bak.2026-03-05-214058` 确认当时设定：
  - baseUrl: http://127.0.0.1:11434
  - primary: `ollama/qwen2.5:7b`
  - fallbacks: `ollama/qwen2.5-coder:7b`, `openai-gpt/gpt-5.2`
- 采取动作（均先备份再改）：
  - 2026-03-06 07:49 备份 `openclaw.json.bak.2026-03-06-074931`
  - 补齐 `models.providers.ollama.models`（加入 qwen2.5:7b 与 qwen2.5-coder:7b 的条目），并将这两模型加入 `agents.defaults.models` 允许列表。
  - 将 `agents.defaults.model.primary` 设为 `ollama/qwen2.5:7b`，fallbacks 设为 `ollama/qwen2.5-coder:7b`、`openai-gpt/gpt-5.2`
  - 重启 gateway；出现一次 shutdown timeout/退出码 1，但新进程成功启动并监听 127.0.0.1:18789。
- 另：此前（2026-03-05 19:58）将 `session.dmScope` 设为 `per-channel-peer` 用于私信隔离，并备份 `openclaw.json.bak.2026-03-05-195840`。
