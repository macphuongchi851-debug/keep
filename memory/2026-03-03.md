# 2026-03-03

- 修复 OpenClaw Telegram 连接失败（直连 https://api.telegram.org 超时）。
- 发现本机 clash-verge-service 仅 IPC，无本地代理端口监听，OpenClaw 无法走代理。
- 新增本地代理：systemd user 服务 `mihomo-openclaw.service`，使用 `/usr/bin/verge-mihomo` + 配置 `~/.config/mihomo/openclaw.yaml`，监听 `127.0.0.1:7897`（mixed-port），并禁用 tun。
- OpenClaw 配置：设置 `channels.telegram.proxy = "http://127.0.0.1:7897"`；按规矩已备份 `~/.openclaw/openclaw.json.bak.2026-03-03-072909`。
- 重启 gateway 后自检：`openclaw channels status --probe` 显示 Telegram running/works；`openclaw status --deep` Telegram Health OK。
